# מחיקה אוטומטית של ספרים עם זכויות יוצרים

## רקע

הריפו הזה מכיל אלפי ספרי קודש בפורמט טקסט. מקור הקבצים מגוון — חלקם
ממאגרים פתוחים (Sefaria, Dicta, Ben-Yehuda וכו'), וחלקם ממקורות שאין לנו
זכויות הפצה עליהם:

- `Tashma`
- `MoreBooks`
- `wiki_jewish_books`

הספרים שמגיעים משלושת המקורות הללו לא אמורים להישאר בריפו. ה-PR הזה מוסיף
מנגנון שמסיר אותם **אוטומטית בכל push** ל-`main`, באמצעות:

1. סקריפט פייתון — `delete_by_source.py`
2. GitHub Actions workflow — `.github/workflows/delete-non-copyrighted.yml`

---

## מקור האמת: SourcesBooks.csv

המידע על מקור כל ספר נמצא בקובץ `SourcesBooks.csv` שמתוחזק בריפו
[`Otzaria/otzaria-library`](https://github.com/Otzaria/otzaria-library)
(לא בריפו הזה).

מבנה הקובץ:

| עמודה            | תוכן                                                |
|------------------|------------------------------------------------------|
| `שם הקובץ`       | למשל `דברי יואל על המועדים.txt`                       |
| `נתיב הקובץ`     | למשל `אוצריא/חסידות/סאטמר/דברי יואל על המועדים.txt`  |
| `תיקיית המקור`   | למשל `Tashma`, `MoreBooks`, `Sefaria` וכו'           |
| `מספר שורות`     | מספר שורות בקובץ                                     |

ה-CSV נשאב חי בכל ריצה מכתובת ה-raw של otzaria-library, ולכן אין צורך
להחזיק אותו בריפו הזה. כשיתווספו ספרים חדשים ל-CSV עם מקור "אסור" — הם
ייכנסו אוטומטית לרשימת המחיקה בריצה הבאה.

---

## `delete_by_source.py` — מה הוא עושה

סקריפט עצמאי שמבצע את המחיקה. שלבי הריצה:

1. **שואב את `SourcesBooks.csv`** — ברירת המחדל היא הורדה מ-raw URL של
   otzaria-library. ניתן לעקוף עם `--csv <נתיב מקומי>` או `--url <כתובת>`.
2. **קורא את ה-CSV** ועובר שורה-שורה.
3. עבור כל שורה — בודק את עמודת `תיקיית המקור`:
   - אם הערך הוא `Tashma`, `MoreBooks` או `wiki_jewish_books` → הקובץ
     מסומן למחיקה.
   - אחרת — דילוג.
4. **נירמול נתיב:** לוקח את העמודה `נתיב הקובץ` (לדוגמא
   `אוצריא/חסידות/סאטמר/דברי יואל על המועדים.txt`) ומסיר את הקידומת
   `אוצריא/`, כי בריפו הזה התיקייה השורשית `אוצריא/` לא קיימת — תיקיות
   הקטגוריה (`חסידות`, `הלכה` וכו') יושבות ישירות בשורש הריפו.
5. **בדיקת בטיחות:** מוודא שהנתיב הסופי נמצא בתוך תיקיית הריפו. אם
   ניסיון לנתיב חורג (`..` וכו') — מדלג ומתריע.
6. **מחיקה:** אם הקובץ קיים — נמחק. אם לא קיים — מסומן ב-`[לא קיים]`.
7. בסוף — מדפיס סיכום: כמה סומנו, כמה נמחקו, כמה כבר לא היו קיימים.

### דגלים נתמכים

| דגל            | תיאור                                                       |
|----------------|--------------------------------------------------------------|
| `--dry-run`    | מדפיס מה היה נמחק, בלי למחוק בפועל.                          |
| `--csv <path>` | קורא CSV מקומי במקום להוריד מ-URL.                           |
| `--url <url>`  | מחליף את ה-raw URL ברירת המחדל.                              |

### דוגמת שימוש מקומית

```bash
# בדיקה בלי למחוק:
python delete_by_source.py --dry-run

# הרצה אמיתית (מורידה CSV מ-GitHub ומוחקת):
python delete_by_source.py
```

---

## `.github/workflows/delete-non-copyrighted.yml` — מה הוא עושה

GitHub Actions workflow שמריץ את הסקריפט אוטומטית. הוא מופעל ב:

- `push` לענף `main`
- `workflow_dispatch` (הרצה ידנית מה-UI של GitHub)

### שלבי הריצה

1. **`actions/checkout@v4`** — מוריד את הריפו, עם `GITHUB_TOKEN` שמורשה
   לכתוב חזרה (`permissions: contents: write`).
2. **`actions/setup-python@v5`** — מתקין פייתון 3.x.
3. **`python delete_by_source.py`** — הסקריפט מוריד את ה-CSV מ-raw URL
   ומוחק את כל הקבצים הרלוונטיים.
4. **commit + push חזרה לענף**, *רק אם היו שינויים*:
   - `git status --porcelain` ריק → סיום בלי commit.
   - אחרת → commit בשם הזהות `github-actions[bot]` עם ההודעה:

     ```
     מחיקת ספרים עם זכויות יוצרים [skip ci]
     ```

### מניעת לולאת push אינסופית

ה-workflow עושה push מתוך עצמו, מה שמסכן בהפעלה רקורסיבית של אותו
workflow על הקומיט החדש. יש שתי שכבות הגנה:

1. **ברמת ה-job:**
   ```yaml
   if: github.actor != 'github-actions[bot]'
   ```
   אם הקומיט שגרם להפעלה נוצר ע"י ה-bot עצמו — ה-job לא רץ כלל.

2. **ברמת ההודעה:** הקומיט מסתיים ב-`[skip ci]`. GitHub Actions
   מדלגים מטבעם על workflows כשהביטוי הזה נמצא בהודעת הקומיט.

בנוסף, `concurrency: group: delete-non-copyrighted` מבטיח שריצה אחת
תסיים לפני שהבאה מתחילה.

### הרשאות

ה-workflow צריך הרשאת כתיבה כדי לעשות commit חזרה. זה מסופק על-ידי:

```yaml
permissions:
  contents: write
```

זה מעניק את ההרשאה ל-`GITHUB_TOKEN` הסטנדרטי של GitHub Actions — לא נדרש
PAT (Personal Access Token) או secret נוסף.

---

## איך לבדוק שזה עובד

לאחר מיזוג ה-PR, מומלץ:

1. להריץ ידנית מ-Actions → **מחיקת ספרים בלי זכויות יוצרים** → **Run workflow**.
2. לוודא שנוצר קומיט חדש בשם `github-actions[bot]` עם ההודעה המתאימה.
3. לוודא שה-workflow לא נכנס ללולאה (הקומיט של ה-bot לא מפעיל הרצה
   נוספת).

---

## קבצים שמשתנים ב-PR הזה

- `delete_by_source.py` — חדש.
- `.github/workflows/delete-non-copyrighted.yml` — חדש.
- מחיקה של כל הקבצים שמקורם `Tashma` / `MoreBooks` / `wiki_jewish_books`
  לפי ה-CSV הנוכחי (~196 קבצים, חלקם כבר לא היו קיימים בריפו).
